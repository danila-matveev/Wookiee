---
phase: 05-verification
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - agents/oleg/playbooks/templates/marketing_weekly.md
  - agents/oleg/playbooks/templates/marketing_monthly.md
  - agents/oleg/playbooks/templates/funnel_weekly.md
  - agents/oleg/playbooks/templates/dds.md
  - agents/oleg/playbooks/templates/localization.md
  - agents/oleg/pipeline/report_pipeline.py
  - shared/data_layer.py
autonomous: false
requirements: [RPT-04, RPT-05, RPT-06, RPT-07, RPT-08, VER-01]

must_haves:
  truths:
    - "Marketing weekly report (RPT-04) generated with campaign data, CTR, DRR, budget"
    - "Marketing monthly report (RPT-05) generated with monthly marketing analysis"
    - "Funnel weekly report (RPT-06) generated with conversion stages data"
    - "DDS weekly report (RPT-07) generated — with real data if finolog tables populated, or documented as known limitation"
    - "Localization weekly report (RPT-08) generated — with real data if wb_localization data available, or documented as known limitation"
    - "All 8 report types have been generated at least once on real data (VER-01)"
  artifacts:
    - path: "agents/oleg/playbooks/templates/marketing_weekly.md"
      provides: "Verified marketing weekly template"
    - path: "agents/oleg/playbooks/templates/marketing_monthly.md"
      provides: "Verified marketing monthly template"
    - path: "agents/oleg/playbooks/templates/funnel_weekly.md"
      provides: "Verified funnel template"
    - path: "agents/oleg/playbooks/templates/dds.md"
      provides: "Verified DDS template"
    - path: "agents/oleg/playbooks/templates/localization.md"
      provides: "Verified localization template"
  key_links:
    - from: "scripts/run_report.py --type marketing_weekly"
      to: "Notion published page"
      via: "report_pipeline.run_report() with Marketer agent"
      pattern: "run_report.*ReportType\\.MARKETING"
    - from: "scripts/run_report.py --type funnel_weekly"
      to: "Notion published page"
      via: "report_pipeline.run_report() with FunnelAgent"
      pattern: "run_report.*ReportType\\.FUNNEL"
---

<objective>
Verify the remaining 5 specialist report types (marketing weekly/monthly, funnel, DDS, localization) against real production data, diagnose and fix issues, and confirm all 8 types have been generated successfully (VER-01).

Purpose: Complete the verification cycle for all report types. Marketing and funnel reports use different agents (Marketer, FunnelAgent) and different data tools, so they may have distinct failure modes from the financial reports verified in Plan 01. DDS and localization are data-driven with external dependencies that may have limited data availability.

Output:
- 5 more verified reports published to Notion
- Any template/pipeline/data-layer fixes committed
- VER-01 confirmed: all 8 types generated on real data
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-verification/05-CONTEXT.md
@.planning/phases/05-verification/05-RESEARCH.md
@.planning/phases/05-verification/05-01-SUMMARY.md

@scripts/run_report.py
@agents/oleg/pipeline/report_pipeline.py
@agents/oleg/pipeline/report_types.py
@agents/oleg/playbooks/templates/marketing_weekly.md
@agents/oleg/playbooks/templates/marketing_monthly.md
@agents/oleg/playbooks/templates/funnel_weekly.md
@agents/oleg/playbooks/templates/dds.md
@agents/oleg/playbooks/templates/localization.md
@agents/oleg/playbooks/data-map.md
@shared/data_layer.py
@shared/notion_client.py
@.planning/phases/05-verification/05-reference-standards.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify marketing reports (RPT-04, RPT-05)</name>
  <files>
    agents/oleg/playbooks/templates/marketing_weekly.md
    agents/oleg/playbooks/templates/marketing_monthly.md
    agents/oleg/pipeline/report_pipeline.py
    shared/data_layer.py
  </files>
  <read_first>
    .planning/phases/05-verification/05-reference-standards.md
    .planning/phases/05-verification/05-01-SUMMARY.md
    agents/oleg/playbooks/templates/marketing_weekly.md
    agents/oleg/playbooks/templates/marketing_monthly.md
    agents/oleg/pipeline/report_pipeline.py
    agents/oleg/pipeline/report_types.py
    agents/oleg/playbooks/data-map.md
    shared/data_layer.py
    scripts/run_report.py
  </read_first>
  <action>
**Marketing Weekly (RPT-04) — per D-08 order position 4:**

1. Determine date: use last Monday (same approach as Plan 01 weekly). Run:
   `python3 scripts/run_report.py --type marketing_weekly --date <last_monday>`

2. Verify 4 criteria (D-04):
   - **Completeness (D-04.1):** All sections from `marketing_weekly.md` template present with real data. No placeholder-only sections.
   - **Depth (D-04.2):** Marketing weekly should contain: campaign performance data, CTR metrics, DRR by channel, advertising budget utilization. Per D-06 specifics: кампании, CTR, ДРР, бюджет.
   - **Accuracy (D-04.3, D-05):** SQL-verify via `shared/data_layer.py`:
     ```sql
     -- Total advertising spend for the week
     SELECT SUM(advertising_cost)
     FROM advertising
     WHERE date BETWEEN '<week_start>' AND '<week_end>'

     -- DRR calculation (MUST have internal/external split per AGENTS.md)
     SELECT SUM(advertising_cost) / NULLIF(SUM(orders_rub), 0) * 100 as drr
     FROM advertising
     WHERE date BETWEEN '<week_start>' AND '<week_end>'
     ```
     Compare with report DRR figure. Must be within 5%.
     Adequacy: DRR between 0-100%, ad spend > 0.
   - **Format (D-04.4):** Toggle headings, Russian, matches reference standard structure from `05-reference-standards.md`.

3. Marketing-specific checks:
   - DRR MUST have internal (МП) / external (блогеры, ВК) split per AGENTS.md
   - Advertising cost increase → check if orders also increased (per analytics.md rule: реклама + рост заказов = эффективно)

4. If issues found — diagnose and fix (D-11):
   - Missing campaign data → check Marketer agent tools and data-map.md mapping
   - No DRR split → strengthen marketing_weekly.md template: add "ОБЯЗАТЕЛЬНО: ДРР с разбивкой — внутренняя (МП реклама) и внешняя (блогеры, ВК)"
   - Shallow analysis → add depth instructions to template

5. Re-run until all criteria pass.

**Marketing Monthly (RPT-05) — per D-08 order position 5:**

1. Determine date: use 1st of current month. Run:
   `python3 scripts/run_report.py --type marketing_monthly --date <month_start>`

2. Verify 4 criteria (D-04):
   - **Completeness:** All sections from `marketing_monthly.md` present
   - **Depth:** Monthly marketing = deeper than weekly. Must include: month-over-month campaign performance trends, budget efficiency analysis, channel comparison, strategic marketing recommendations
   - **Accuracy:** SQL-verify monthly ad spend and DRR totals against report figures within 5%
   - **Format:** Toggle headings, Russian, reference standard match

3. Same DRR split check and advertising→orders effectiveness check.

4. Fix and iterate as needed.

**After both marketing reports verified, run regression:**
```bash
python3 -m pytest tests/agents/oleg/playbooks/ tests/agents/oleg/runner/ -q --tb=short
```
  </action>
  <verify>
    <automated>python3 -m pytest tests/agents/oleg/playbooks/ tests/agents/oleg/runner/ -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/run_report.py --type marketing_weekly --date <date>` exits 0 and outputs Notion URL
    - Marketing weekly report contains campaign/advertising data (not placeholder text)
    - Marketing weekly report contains DRR with internal/external split
    - Marketing weekly report contains CTR or click-through metrics
    - `python3 scripts/run_report.py --type marketing_monthly --date <date>` exits 0 and outputs Notion URL
    - Marketing monthly report contains month-level marketing analysis
    - Marketing monthly report has DRR with internal/external split
    - SQL-verified DRR figures match report within 5%
    - All playbook tests pass: `python3 -m pytest tests/agents/oleg/playbooks/ -q --tb=short` exits 0
  </acceptance_criteria>
  <done>Marketing weekly (RPT-04) and marketing monthly (RPT-05) reports generated, verified against 4 quality criteria, and published to Notion. DRR split and advertising effectiveness analysis confirmed present.</done>
</task>

<task type="auto">
  <name>Task 2: Verify funnel, DDS, and localization reports (RPT-06, RPT-07, RPT-08) and confirm VER-01</name>
  <files>
    agents/oleg/playbooks/templates/funnel_weekly.md
    agents/oleg/playbooks/templates/dds.md
    agents/oleg/playbooks/templates/localization.md
    agents/oleg/pipeline/report_pipeline.py
    shared/data_layer.py
  </files>
  <read_first>
    .planning/phases/05-verification/05-reference-standards.md
    .planning/phases/05-verification/05-01-SUMMARY.md
    agents/oleg/playbooks/templates/funnel_weekly.md
    agents/oleg/playbooks/templates/dds.md
    agents/oleg/playbooks/templates/localization.md
    agents/oleg/pipeline/report_pipeline.py
    agents/oleg/pipeline/report_types.py
    agents/oleg/playbooks/data-map.md
    shared/data_layer.py
    scripts/run_report.py
  </read_first>
  <action>
**Funnel Weekly (RPT-06) — per D-08 order position 6:**

1. Run: `python3 scripts/run_report.py --type funnel_weekly --date <last_monday>`

2. Verify 4 criteria (D-04):
   - **Completeness:** All sections from `funnel_weekly.md` template present
   - **Depth:** Funnel reports show conversions by stage (per D-06). Must include: traffic/impressions → clicks → cart → orders → deliveries → buyback stages with conversion percentages between stages
   - **Accuracy:** SQL-verify funnel data:
     ```sql
     -- Check orders and revenue for the week (WB only — funnel is WB marketplace)
     SELECT SUM(orders_rub), SUM(revenue_rub)
     FROM fin_data
     WHERE date BETWEEN '<week_start>' AND '<week_end>'
     ```
     Verify total orders figure appears in report within 5%.
     Note: Выкуп% in funnel is INFORMATIONAL only — it must NOT be used as causation driver per AGENTS.md (but showing it as a funnel stage metric is acceptable).
   - **Format:** Toggle headings, Russian, reference standard match

3. If issues found:
   - Empty funnel stages → check FunnelAgent tools, `build_funnel_report` tool
   - Missing conversion rates → strengthen funnel_weekly.md template with explicit stage listing
   - Fix and re-run.

**DDS Weekly / Finolog (RPT-07) — per D-08 order position 7:**

1. **Pre-check data availability** (Pitfall 2 from RESEARCH.md):
   Check if finolog data exists in the DB:
   ```python
   # Query via shared/data_layer.py
   # Check finolog tables for recent data
   ```
   If finolog tables are empty or have no recent data:
   - Document this as a known limitation in the SUMMARY
   - Still run the report to verify structural correctness
   - Verify the template structure is correct and the report generates without errors (even if content is placeholder)

2. Run: `python3 scripts/run_report.py --type finolog_weekly --date <last_monday>`

3. Verify (conditional on data availability):
   - **If data exists:** Full 4-criteria check. DDS per D-06: поступления/списания по категориям.
   - **If no data:** Verify report generates without error (exit 0), structure matches `dds.md` template, graceful degradation placeholders are in Russian, and document the limitation.

4. The DDS template (`dds.md`) is data-driven with no LLM depth markers — verify this characteristic is preserved (no hallucinated analysis when data is absent).

**Localization Weekly (RPT-08) — per D-08 order position 8:**

1. **Pre-check data availability** (Pitfall 6 from RESEARCH.md):
   Check if wb_localization data exists:
   ```python
   # Check for recent localization/logistics data in the DB
   # Gate checker has soft gate for logistics data
   ```
   If no recent data:
   - Document as known limitation
   - Still run to verify structural correctness

2. Run: `python3 scripts/run_report.py --type localization_weekly --date <last_monday>`

3. Verify (conditional on data availability):
   - **If data exists:** Full 4-criteria check. Per D-06: логистические расходы WB.
   - **If no data:** Same structural verification approach as DDS.

4. Localization template (`localization.md`) is also data-driven — verify no hallucinated analysis when data is absent.

**VER-01 Confirmation — All 8 types generated:**

After all 5 specialist reports are verified (this task) + 3 financial reports (Plan 01), create a verification summary listing all 8 types with their generation status:

```
| Type | Status | Notion URL | Date | Quality Notes |
|------|--------|------------|------|---------------|
| daily | PASS | ... | ... | ... |
| weekly | PASS | ... | ... | ... |
| monthly | PASS | ... | ... | ... |
| marketing_weekly | PASS | ... | ... | ... |
| marketing_monthly | PASS | ... | ... | ... |
| funnel_weekly | PASS | ... | ... | ... |
| finolog_weekly | PASS/LIMITED | ... | ... | ... |
| localization_weekly | PASS/LIMITED | ... | ... | ... |
```

Status = PASS (all 4 criteria met) or LIMITED (generated structurally correct but data dependency not available).

**Final regression run:**
```bash
python3 -m pytest tests/ -q --tb=short
```
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/run_report.py --type funnel_weekly --date <date>` exits 0
    - Funnel report contains conversion stage data (not all placeholder)
    - `python3 scripts/run_report.py --type finolog_weekly --date <date>` exits 0 (even if data limited)
    - `python3 scripts/run_report.py --type localization_weekly --date <date>` exits 0 (even if data limited)
    - For DDS and localization: either full data verification OR documented known limitation with structural correctness confirmed
    - All 8 report types have been generated at least once (VER-01 confirmed)
    - `python3 -m pytest tests/ -q --tb=short` exits 0
  </acceptance_criteria>
  <done>Funnel weekly (RPT-06), DDS weekly (RPT-07), and localization weekly (RPT-08) verified. All 8 report types generated on real data (VER-01). Any data-dependent limitations documented. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final verification of all specialist reports and VER-01 completion</name>
  <files>none</files>
  <action>
Human verification checkpoint. 5 specialist reports were generated and verified: marketing weekly, marketing monthly, funnel weekly, DDS weekly, localization weekly. Combined with Plan 01's 3 financial reports, all 8 types have been generated (VER-01). Any template/pipeline fixes committed. DDS and localization may have limited data documented.

Verification steps:
1. Open marketing weekly Notion page. Verify: campaign data present, DRR with split, CTR metrics
2. Open marketing monthly Notion page. Verify: deeper than weekly, month-over-month trends, budget analysis
3. Open funnel weekly Notion page. Verify: conversion stages with percentages, order/revenue data
4. Open DDS weekly Notion page. Verify: either real financial categories or documented limitation
5. Open localization weekly Notion page. Verify: either logistics cost data or documented limitation
6. Confirm all 8 report types have been generated — VER-01 is complete
7. Review the verification summary table showing all 8 types with status

Resume: Type "approved" if all reports look correct and VER-01 is confirmed, or describe specific issues.
  </action>
  <verify>User confirms "approved" or provides feedback for iteration</verify>
  <done>Human has reviewed all 5 specialist reports in Notion and confirmed VER-01 (all 8 types generated and verified).</done>
</task>

</tasks>

<verification>
- All 5 specialist report types generated (exit code 0)
- Marketing reports contain DRR split and campaign metrics
- Funnel report contains conversion stages
- DDS and localization either fully verified or documented as limited
- All 8 types total generated at least once on real data (VER-01)
- All existing tests pass after any fixes
- Human verified specialist report quality in Notion
</verification>

<success_criteria>
1. Marketing weekly and monthly reports published to Notion with real advertising/campaign data
2. Funnel weekly report published with conversion stage data
3. DDS and localization reports generated (with real data or documented limitation)
4. All 8 report types confirmed generated on real data (VER-01 complete)
5. Reference standards from Plan 01 used as quality bar for comparisons
6. Any template or pipeline fixes committed and tested
7. Human approves all specialist reports
</success_criteria>

<output>
After completion, create `.planning/phases/05-verification/05-02-SUMMARY.md`
</output>
