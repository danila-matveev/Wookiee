---
phase: 03-table-view
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - wookiee-hub/src/components/matrix/data-table.tsx
  - wookiee-hub/src/components/matrix/table-cell.tsx
  - wookiee-hub/src/pages/product-matrix/models-page.tsx
  - wookiee-hub/src/pages/product-matrix/articles-page.tsx
  - wookiee-hub/src/pages/product-matrix/products-page.tsx
autonomous: false
requirements:
  - TABLE-01
  - TABLE-02
  - TABLE-03
  - TABLE-07

must_haves:
  truths:
    - "Every column header shows display_name from FieldDefinition, not technical field_name"
    - "Reference columns (категория, фабрика, коллекция) show resolved human names, not dashes or IDs"
    - "Status column shows colored Badge: green for Активный, gray for Архив"
    - "Archived rows have opacity-60 visual dimming"
  artifacts:
    - path: "wookiee-hub/src/components/matrix/data-table.tsx"
      provides: "DataTable with sort callbacks, status badge rendering, archive row styling"
      contains: "onSort"
    - path: "wookiee-hub/src/pages/product-matrix/models-page.tsx"
      provides: "Models page using fieldDefsToColumns + useTableState"
      contains: "fieldDefsToColumns"
  key_links:
    - from: "wookiee-hub/src/pages/product-matrix/models-page.tsx"
      to: "wookiee-hub/src/hooks/use-table-state.ts"
      via: "useTableState hook call"
      pattern: "useTableState"
    - from: "wookiee-hub/src/pages/product-matrix/models-page.tsx"
      to: "wookiee-hub/src/lib/field-def-columns.ts"
      via: "fieldDefsToColumns for column generation"
      pattern: "fieldDefsToColumns"
    - from: "wookiee-hub/src/components/matrix/data-table.tsx"
      to: "wookiee-hub/src/components/ui/badge.tsx"
      via: "Badge component for status rendering"
      pattern: "Badge"
---

<objective>
Wire FieldDef-driven columns, sort indicators, status badges, and archive row styling into the DataTable and all three entity pages.

Purpose: This is the core table display overhaul — replacing hardcoded column arrays with FieldDefinition-driven columns, adding sort interaction to column headers, rendering status as colored badges, and dimming archived rows. After this plan, the table looks correct and is interactive for sorting.

Output: DataTable supports sort callbacks and status/archive rendering; all three page components use useTableState + fieldDefsToColumns instead of hardcoded columns.
</objective>

<execution_context>
@/Users/danilamatveev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilamatveev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-table-view/03-CONTEXT.md
@.planning/phases/03-table-view/03-RESEARCH.md
@.planning/phases/03-table-view/03-01-SUMMARY.md

@wookiee-hub/src/components/matrix/data-table.tsx
@wookiee-hub/src/components/matrix/table-cell.tsx
@wookiee-hub/src/pages/product-matrix/models-page.tsx
@wookiee-hub/src/pages/product-matrix/articles-page.tsx
@wookiee-hub/src/pages/product-matrix/products-page.tsx
@wookiee-hub/src/hooks/use-table-state.ts
@wookiee-hub/src/lib/field-def-columns.ts
@wookiee-hub/src/components/matrix/panel/types.ts
@wookiee-hub/src/lib/matrix-api.ts
@wookiee-hub/src/stores/matrix-store.ts

<interfaces>
<!-- From Plan 01 outputs — executor uses these directly -->

From use-table-state.ts:
```typescript
export interface TableSortState { field: string | null; order: "asc" | "desc" }
export interface TableState {
  page: number; perPage: number; sort: TableSortState; hiddenFields: Set<string>;
  setPage: (p: number) => void; toggleSort: (field: string) => void;
  toggleFieldVisibility: (fn: string) => void;
  apiParams: Record<string, string | number | undefined>;
}
export function useTableState(entity: string, defaultPerPage?: number): TableState
```

From field-def-columns.ts:
```typescript
export function fieldDefsToColumns<T>(
  defs: FieldDefinition[], lookupCache: Record<string, { id: number; nazvanie: string }[]>,
  hiddenFields: Set<string>
): Column<T>[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DataTable sort, status badge, and archive row styling</name>
  <files>wookiee-hub/src/components/matrix/data-table.tsx, wookiee-hub/src/components/matrix/table-cell.tsx</files>
  <action>
    1. **DataTable props** — Add to DataTable component props:
       - `onSort?: (fieldName: string) => void` — callback when column header is clicked
       - `sortState?: TableSortState` — current sort field and order for indicator rendering
       Import `TableSortState` type from `@/hooks/use-table-state`.

    2. **Column header sort interaction** — In the `<th>` rendering:
       - If `onSort` is provided and column has a `fieldDef`, make header clickable: `onClick={() => onSort(column.fieldDef.field_name)}`
       - Show sort indicator icon next to label: `ArrowUp` for asc, `ArrowDown` for desc (from lucide-react), `ArrowUpDown` for unsorted columns. Only show active indicator on the column matching `sortState.field`.
       - Add `cursor-pointer select-none` classes to sortable headers.

    3. **Status badge rendering** — In the cell rendering logic:
       - Detect status column by checking if the column key is `status_name` (or column.fieldDef?.field_name === "status_id")
       - Render status values using shadcn `Badge`: green styling (`bg-green-100 text-green-800 border-green-200`) for "Активный", gray styling (`bg-gray-100 text-gray-600 border-gray-200`) for "Архив"
       - Import `Badge` from `@/components/ui/badge`

    4. **Archive row styling** — On the `<tr>` element:
       - Check if row has `status_name === "Архив"` (cast row to Record<string, unknown>)
       - Add `opacity-60` class to archived rows
       - Use `cn()` to conditionally apply

    5. **Simplify table-cell.tsx** — Per Claude's discretion from CONTEXT: remove inline editing logic from TableCell. All editing happens in Detail Panel (consistent with Phase 2 decision). TableCell becomes a pure read-only renderer.

    Use `frontend-design:frontend-design` skill for badge styling and sort indicator design. Follow Pattern 4 from 03-RESEARCH.md for badge implementation.
  </action>
  <verify>
    <automated>cd /Users/danilamatveev/Desktop/Документы/Cursor/Wookiee/wookiee-hub && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>DataTable renders sort indicators on column headers, status badges in status column, and opacity-60 on archived rows. TableCell is read-only. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: FieldDef-driven columns in all three page components</name>
  <files>wookiee-hub/src/pages/product-matrix/models-page.tsx, wookiee-hub/src/pages/product-matrix/articles-page.tsx, wookiee-hub/src/pages/product-matrix/products-page.tsx</files>
  <action>
    1. **models-page.tsx** refactor:
       - Import `useTableState` from `@/hooks/use-table-state`
       - Import `fieldDefsToColumns` from `@/lib/field-def-columns`
       - Call `const tableState = useTableState("models")` at top of component
       - Replace hardcoded `Column[]` array with `fieldDefsToColumns(fieldDefs, lookupCache, tableState.hiddenFields)` — where `fieldDefs` comes from the schema endpoint and `lookupCache` from Zustand store
       - Pass `tableState.apiParams` to the `matrixApi.listModels()` call in `useApiQuery`
       - Include `[tableState.page, tableState.sort.field, tableState.sort.order]` in the useApiQuery deps array so it re-fetches on sort/page change
       - Pass `onSort={tableState.toggleSort}` and `sortState={tableState.sort}` to `<DataTable>`
       - Remove the old hardcoded column definitions

    2. **articles-page.tsx** — Same refactor pattern as models-page. Use `useTableState("articles")`. Call `matrixApi.listArticles(tableState.apiParams)`.

    3. **products-page.tsx** — Same refactor pattern. Use `useTableState("products")`. Call `matrixApi.listProducts(tableState.apiParams)`.

    IMPORTANT: Keep existing row click → detail panel open behavior. Keep existing row selection/expand behavior. Only replace the column generation and data fetching parts.

    IMPORTANT: Reference field columns must use `_name` keys (e.g., `kategoriya_name` not `kategoriya_id`) — the backend already returns these pre-joined values. The `fieldDefsToColumns` utility handles this mapping. Do NOT add extra lookup resolution at render time for table cells.
  </action>
  <verify>
    <automated>cd /Users/danilamatveev/Desktop/Документы/Cursor/Wookiee/wookiee-hub && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All three entity pages use FieldDef-driven columns via fieldDefsToColumns. Column headers show display_name. Reference fields show resolved names. Sort interaction triggers re-fetch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>FieldDef-driven table columns with sort interaction, status badges, and archive row styling across all three entity pages (models, articles, products).</what-built>
  <how-to-verify>
    1. Open http://localhost:5173/product/matrix (or the dev URL)
    2. On Models tab: verify column headers show Russian display names ("Код", "Категория", etc.), NOT technical names ("kod", "kategoriya_id")
    3. Verify "Категория", "Коллекция", "Фабрика" columns show real names (e.g., "Верхняя одежда"), NOT "—" or numeric IDs
    4. Verify status column shows colored Badge: green "Активный", gray "Архив"
    5. Verify archived rows (status = Архив) appear with reduced opacity
    6. Click a column header → verify table re-sorts ascending; click again → descending; sort arrow indicator visible
    7. Switch to Articles and Products tabs — verify same behavior
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd wookiee-hub && npx tsc --noEmit` — zero errors
- Visual: All column headers are human-readable display_name values
- Visual: Reference columns show resolved names
- Visual: Status badges are green/gray
- Visual: Archived rows dimmed
- Interaction: Column header click triggers sort with indicator
</verification>

<success_criteria>
- Column headers use FieldDefinition.display_name (TABLE-01)
- Reference fields show resolved human names (TABLE-02)
- Status column renders colored Badge (TABLE-03)
- Archived rows have opacity-60 styling (TABLE-07)
- Sort interaction works end-to-end: click header → API call with sort params → re-rendered sorted data
</success_criteria>

<output>
After completion, create `.planning/phases/03-table-view/03-02-SUMMARY.md`
</output>
