---
phase: 03-table-view
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - wookiee-hub/src/components/matrix/column-visibility-popover.tsx
  - wookiee-hub/src/components/matrix/create-record-dialog.tsx
  - wookiee-hub/src/components/matrix/pagination-controls.tsx
  - wookiee-hub/src/components/matrix/matrix-topbar.tsx
  - wookiee-hub/src/pages/product-matrix/models-page.tsx
  - wookiee-hub/src/pages/product-matrix/articles-page.tsx
  - wookiee-hub/src/pages/product-matrix/products-page.tsx
autonomous: false
requirements:
  - TABLE-05
  - TABLE-06
  - CRUD-01
  - CRUD-02

must_haves:
  truths:
    - "Pagination controls appear below the table with page navigation and total count"
    - "Column visibility popover lets user show/hide columns via checkboxes"
    - "Click '+ Создать' opens a modal dialog with entity-appropriate form fields"
    - "Submitting create form creates the record, refreshes the table, and opens Detail Panel for the new record"
  artifacts:
    - path: "wookiee-hub/src/components/matrix/pagination-controls.tsx"
      provides: "Pagination bar component"
      exports: ["PaginationControls"]
    - path: "wookiee-hub/src/components/matrix/column-visibility-popover.tsx"
      provides: "Column visibility toggle popover"
      exports: ["ColumnVisibilityPopover"]
    - path: "wookiee-hub/src/components/matrix/create-record-dialog.tsx"
      provides: "Entity-aware create record dialog"
      exports: ["CreateRecordDialog"]
  key_links:
    - from: "wookiee-hub/src/components/matrix/pagination-controls.tsx"
      to: "wookiee-hub/src/hooks/use-table-state.ts"
      via: "setPage callback from useTableState"
      pattern: "setPage"
    - from: "wookiee-hub/src/components/matrix/column-visibility-popover.tsx"
      to: "wookiee-hub/src/hooks/use-table-state.ts"
      via: "toggleFieldVisibility and hiddenFields from useTableState"
      pattern: "toggleFieldVisibility"
    - from: "wookiee-hub/src/components/matrix/create-record-dialog.tsx"
      to: "wookiee-hub/src/lib/matrix-api.ts"
      via: "matrixApi.createModel/createArticle/createProduct"
      pattern: "matrixApi\\.create"
---

<objective>
Pagination controls, column visibility popover, and create record dialog — the interactive table features.

Purpose: Completes the table view by adding pagination (replacing fixed 200-row fetch), column toggle (replacing ManageFieldsDialog), and record creation. These are the three user-facing interactive features that make the table a functional working tool.

Output: PaginationControls component, ColumnVisibilityPopover component, CreateRecordDialog component, topbar integration with "+ Создать" button and "Настроить поля" button.
</objective>

<execution_context>
@/Users/danilamatveev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilamatveev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-table-view/03-CONTEXT.md
@.planning/phases/03-table-view/03-RESEARCH.md
@.planning/phases/03-table-view/03-01-SUMMARY.md

@wookiee-hub/src/components/matrix/matrix-topbar.tsx
@wookiee-hub/src/components/matrix/manage-fields-dialog.tsx
@wookiee-hub/src/components/matrix/data-table.tsx
@wookiee-hub/src/pages/product-matrix/models-page.tsx
@wookiee-hub/src/hooks/use-table-state.ts
@wookiee-hub/src/lib/field-def-columns.ts
@wookiee-hub/src/lib/matrix-api.ts
@wookiee-hub/src/stores/matrix-store.ts
@wookiee-hub/src/components/matrix/panel/types.ts

<interfaces>
<!-- From Plan 01 outputs -->

From use-table-state.ts:
```typescript
export interface TableState {
  page: number; perPage: number; sort: TableSortState; hiddenFields: Set<string>;
  setPage: (p: number) => void; toggleSort: (field: string) => void;
  toggleFieldVisibility: (fn: string) => void;
  apiParams: Record<string, string | number | undefined>;
}
```

From matrix-api.ts (existing):
```typescript
// PaginatedResponse shape from backend:
{ items: T[]; total: number; page: number; per_page: number; pages: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PaginationControls, ColumnVisibilityPopover, and CreateRecordDialog components</name>
  <files>wookiee-hub/src/components/matrix/pagination-controls.tsx, wookiee-hub/src/components/matrix/column-visibility-popover.tsx, wookiee-hub/src/components/matrix/create-record-dialog.tsx</files>
  <action>
    1. **PaginationControls** (`pagination-controls.tsx`):
       - Props: `page: number`, `pages: number`, `total: number`, `perPage: number`, `onPageChange: (page: number) => void`
       - Render: "Показано X-Y из Z" label + page buttons: « Prev | page numbers (max 5 visible with ellipsis) | Next »
       - Use shadcn `Button` with `variant="ghost"` for page buttons, `variant="outline"` for active page
       - Disable Prev on page 1, disable Next on last page
       - Compact design — fits a single line below the table

    2. **ColumnVisibilityPopover** (`column-visibility-popover.tsx`):
       - Props: `fields: FieldDefinition[]`, `hiddenFields: Set<string>`, `onToggle: (fieldName: string) => void`
       - Render: shadcn `Popover` triggered by `Button` with `Settings2` icon and "Настроить поля" label
       - Inside: list of checkboxes, one per FieldDefinition (only `is_visible` fields), checked = column visible
       - Follow Pattern 6 from 03-RESEARCH.md exactly
       - Import FieldDefinition type from `@/lib/matrix-api`

    3. **CreateRecordDialog** (`create-record-dialog.tsx`):
       - Props: `entityType: string`, `fieldDefs: FieldDefinition[]`, `lookupCache`, `open: boolean`, `onOpenChange`, `onCreated: (id: number) => void`
       - Render: shadcn `Dialog` with form fields generated from FieldDefinitions
       - Field selection logic: show fields where `!is_system` AND field_type is one of (text, number, reference, select) AND field_name does NOT end in `_name` (those are computed joins)
       - For entity "models": show at minimum `kod` (text, required), `kategoriya_id` (select), `kollekciya_id` (select), `fabrika_id` (select), `status_id` (select)
       - For reference/select fields: populate options from `lookupCache[LOOKUP_TABLE_MAP[field_name]]`
       - Use shadcn `Select` for lookup fields, `Input` for text/number fields
       - On submit: call appropriate `matrixApi.create*()` method based on entityType. On success: call `onCreated(newRecord.id)`. On error: show error message in the dialog.
       - Form validation: `required` HTML attribute on essential fields. No react-hook-form needed.
       - Use `import type` for type-only imports.

    Use `frontend-design:frontend-design` skill for component styling. All components must use shadcn primitives (Button, Popover, Dialog, Badge, Checkbox, Select, Input).
  </action>
  <verify>
    <automated>cd /Users/danilamatveev/Desktop/Документы/Cursor/Wookiee/wookiee-hub && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Three new components created and compile without errors. PaginationControls renders page navigation. ColumnVisibilityPopover renders Notion-style checkbox list. CreateRecordDialog renders entity-aware form with lookup selects.</done>
</task>

<task type="auto">
  <name>Task 2: Wire components into topbar and page components</name>
  <files>wookiee-hub/src/components/matrix/matrix-topbar.tsx, wookiee-hub/src/pages/product-matrix/models-page.tsx, wookiee-hub/src/pages/product-matrix/articles-page.tsx, wookiee-hub/src/pages/product-matrix/products-page.tsx</files>
  <action>
    1. **matrix-topbar.tsx**:
       - Add "+ Создать" button (shadcn `Button` with `Plus` icon from lucide-react)
       - Add `ColumnVisibilityPopover` in place of the current ManageFieldsDialog trigger
       - Props needed from parent: `fieldDefs`, `hiddenFields`, `onToggleField`, `onCreateClick`
       - Remove or deprecate the ManageFieldsDialog import (keep the file for now, just stop rendering it)

    2. **models-page.tsx** (already refactored in Plan 02 for columns/sort):
       - Add `<PaginationControls>` below the `<DataTable>`, passing `page={tableState.page}`, `pages={data?.pages}`, `total={data?.total}`, `perPage={tableState.perPage}`, `onPageChange={tableState.setPage}`
       - Add `<CreateRecordDialog>` with `open` state, passing `entityType="models"`, `fieldDefs`, `lookupCache`, and `onCreated` handler
       - `onCreated` handler: reset page to 1, increment refreshKey to trigger re-fetch, call `setDetailPanelId(newId)` to open the new record in Detail Panel
       - Pass `onCreateClick`, `fieldDefs`, `hiddenFields`, `onToggleField` to `MatrixTopbar` (or render topbar actions locally)
       - Add `refreshKey` state: `const [refreshKey, setRefreshKey] = useState(0)` and include in useApiQuery deps

    3. **articles-page.tsx** — Same wiring as models-page with `entityType="articles"` and `matrixApi.listArticles`.

    4. **products-page.tsx** — Same wiring with `entityType="products"` and `matrixApi.listProducts`.

    IMPORTANT: The `data` object from `useApiQuery` is the full PaginatedResponse — destructure `items`, `total`, `page`, `pages` from it. Pass `data.items` to DataTable, pass pagination metadata to PaginationControls.

    NOTE on file overlap with Plan 02: Plan 02 refactors the column generation and sort wiring in `*-page.tsx`. This task adds pagination, create dialog, and column visibility ON TOP of that refactored code. If Plan 02 runs first, read its SUMMARY to understand the current state of these files. If running in parallel, coordinate by keeping changes to separate sections of each file.
  </action>
  <verify>
    <automated>cd /Users/danilamatveev/Desktop/Документы/Cursor/Wookiee/wookiee-hub && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Pagination controls render below table. "+ Создать" button opens create dialog. "Настроить поля" opens column visibility popover. All wired to all three entity pages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Pagination controls, column visibility toggle, and create record dialog wired into all three entity pages.</what-built>
  <how-to-verify>
    1. Open http://localhost:5173/product/matrix
    2. **Pagination (TABLE-05):** Verify pagination controls appear below the table. Check "Показано X-Y из Z" label is correct. Click page 2 — different data loads. Click Prev/Next — works correctly.
    3. **Column visibility (TABLE-06):** Click "Настроить поля" in topbar — popover opens with column checkboxes. Uncheck a column — it disappears from the table. Re-check — it reappears. Data and sort state are preserved.
    4. **Create record (CRUD-01, CRUD-02):** Click "+ Создать" — dialog opens with form fields. Verify reference fields (категория, коллекция, фабрика) show as select dropdowns with real options. Fill in required fields and submit. Verify: dialog closes, table refreshes, new record appears, Detail Panel opens for the new record.
    5. Switch to Articles and Products tabs — verify same pagination, column toggle, and create behavior.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd wookiee-hub && npx tsc --noEmit` — zero errors
- Visual: Pagination bar with page numbers and total count below table
- Visual: Column visibility popover with checkbox list
- Visual: Create dialog with entity-appropriate form fields and lookup selects
- Interaction: Page navigation loads different data
- Interaction: Column toggle hides/shows columns
- Interaction: Create form submits, record appears in table, Detail Panel opens
</verification>

<success_criteria>
- Table shows paginated data with navigation controls (TABLE-05)
- Column visibility toggle works via Notion-style popover (TABLE-06)
- "+ Создать" button opens entity-aware create form (CRUD-01)
- Create form has required fields and lookup selects for references (CRUD-02)
- After creation: table refreshes and Detail Panel opens for new record
</success_criteria>

<output>
After completion, create `.planning/phases/03-table-view/03-03-SUMMARY.md`
</output>
