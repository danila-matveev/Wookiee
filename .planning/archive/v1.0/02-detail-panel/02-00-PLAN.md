---
phase: 02-detail-panel
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - wookiee-hub/playwright.config.ts
  - wookiee-hub/tests/detail-panel.spec.ts
  - wookiee-hub/tests/fixtures.ts
autonomous: true
requirements: [PANEL-01, PANEL-02, PANEL-03, PANEL-04, PANEL-05, PANEL-06, PANEL-07, PANEL-08]

must_haves:
  truths:
    - "Playwright config exists and targets localhost:25000"
    - "Test stubs for PANEL-01 through PANEL-08 exist and can be discovered by grep"
    - "Shared fixtures provide page helpers for opening panel, switching entity type"
  artifacts:
    - path: "wookiee-hub/playwright.config.ts"
      provides: "Playwright configuration targeting dev server"
      contains: "localhost:25000"
    - path: "wookiee-hub/tests/detail-panel.spec.ts"
      provides: "E2E test stubs for all PANEL requirements"
      contains: "PANEL-01"
    - path: "wookiee-hub/tests/fixtures.ts"
      provides: "Shared page fixtures and test helpers"
      exports: ["test", "expect"]
  key_links:
    - from: "wookiee-hub/tests/detail-panel.spec.ts"
      to: "wookiee-hub/tests/fixtures.ts"
      via: "imports extended test/expect from fixtures"
      pattern: "from.*fixtures"
---

<objective>
Create Playwright test infrastructure for Phase 2: config file, shared fixtures, and test stubs for all PANEL requirements.

Purpose: All plans (02-01 through 02-04) need automated verification. Without test infrastructure, no plan can satisfy the Nyquist rule. This Wave 0 plan creates the scaffolding that subsequent plans' `<verify>` commands depend on.

Output: playwright.config.ts, tests/fixtures.ts, tests/detail-panel.spec.ts with stubs.
</objective>

<execution_context>
@/Users/danilamatveev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilamatveev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-detail-panel/02-CONTEXT.md
@.planning/phases/02-detail-panel/02-RESEARCH.md
@.planning/phases/02-detail-panel/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Playwright config and shared fixtures</name>
  <files>
    wookiee-hub/playwright.config.ts,
    wookiee-hub/tests/fixtures.ts
  </files>
  <action>
**playwright.config.ts**:
Create Playwright config in the wookiee-hub root:
- `baseURL: "http://localhost:25000"` (the dev server port)
- `testDir: "./tests"`
- `use: { trace: "on-first-retry" }`
- Single project: chromium only (fast feedback)
- `retries: 0` for development, `timeout: 30000`
- `webServer` block pointing to `npm run dev` with port 25000 (only if not already running)

**tests/fixtures.ts**:
Create shared test fixtures extending Playwright's base test:
- `openDetailPanel(page, entityType, rowIndex?)` — clicks on a table row to open the detail panel Sheet; waits for `[data-slot="sheet-content"]` to be visible
- `switchEntityType(page, entityType)` — clicks the entity tab to switch to models/articles/products
- `enterEditMode(page)` — clicks the edit button in the panel header
- `getFieldRow(page, fieldName)` — locator for a specific field row by field name
- Export extended `test` and `expect` from fixtures

Use `import type` for any type-only imports.
  </action>
  <verify>
    <automated>cd wookiee-hub && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>
    - playwright.config.ts exists with baseURL localhost:25000
    - tests/fixtures.ts exports test helpers for panel interaction
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for PANEL-01 through PANEL-08</name>
  <files>
    wookiee-hub/tests/detail-panel.spec.ts
  </files>
  <action>
**tests/detail-panel.spec.ts**:
Create test file with stubbed test cases for each requirement. Each test should be a `test.describe` block with `test.skip` or `test.fixme` annotation so they are discoverable but do not fail until implementation exists.

Structure:
```typescript
import { test, expect } from "./fixtures"

test.describe("Detail Panel", () => {
  test.describe("PANEL-01: ModelOsnova read mode", () => {
    test.fixme("shows all ModelOsnova fields grouped in sections", async ({ page }) => {
      // Open model detail panel
      // Verify sections: Основные, Размеры, Логистика, Контент
      // Verify key fields visible: kod, ves_kg, dlina_cm, etc.
    })
  })

  test.describe("PANEL-02: Artikul read mode", () => {
    test.fixme("shows all Artikul fields in read mode", async ({ page }) => {
      // Switch to articles, open detail panel
      // Verify artikul-specific fields visible
    })
  })

  test.describe("PANEL-03: Tovar read mode", () => {
    test.fixme("shows Tovar fields with marketplace IDs as non-editable", async ({ page }) => {
      // Switch to products, open detail panel
      // Verify marketplace ID fields have lock indicator
    })
  })

  test.describe("PANEL-04: Edit mode input types", () => {
    test.fixme("shows correct input type per field_type", async ({ page }) => {
      // Open panel, enter edit mode
      // Verify text fields have text input, number fields have number input, etc.
    })
  })

  test.describe("PANEL-05: Select fields with lookups", () => {
    test.fixme("populates select fields from lookup API", async ({ page }) => {
      // Open panel, enter edit mode
      // Click a select field (e.g. kategoriya)
      // Verify dropdown has options from lookup table
    })
  })

  test.describe("PANEL-06: Save and Cancel", () => {
    test.fixme("save persists changes via PATCH", async ({ page }) => {
      // Open panel, enter edit mode, change a field, click Save
      // Verify field shows new value in read mode
    })
    test.fixme("cancel discards changes", async ({ page }) => {
      // Open panel, enter edit mode, change a field, click Cancel
      // Verify field shows original value
    })
  })

  test.describe("PANEL-07: System fields protection", () => {
    test.fixme("system fields remain text in edit mode", async ({ page }) => {
      // Open panel, enter edit mode
      // Verify barkod, nomenklatura_wb show no input
    })
  })

  test.describe("PANEL-08: Related entities", () => {
    test.fixme("badge counters show related entity counts", async ({ page }) => {
      // Open model panel
      // Verify badge shows "Артикулы: N"
    })
    test.fixme("clicking child in list opens child in panel", async ({ page }) => {
      // Open model panel, expand related section
      // Click a child artikul
      // Verify panel shows artikul data
    })
  })
})
```

Each test.fixme will be progressively unfixed as the corresponding plan implements the feature. This ensures `npx playwright test --grep "PANEL-01"` discovers tests immediately.
  </action>
  <verify>
    <automated>cd wookiee-hub && npx playwright test --list 2>&1 | head -20</automated>
  </verify>
  <done>
    - detail-panel.spec.ts has test stubs for PANEL-01 through PANEL-08
    - `npx playwright test --list` discovers all test cases
    - All tests are marked fixme (not failing, just skipped)
  </done>
</task>

</tasks>

<verification>
- `ls wookiee-hub/playwright.config.ts wookiee-hub/tests/detail-panel.spec.ts wookiee-hub/tests/fixtures.ts` — all three files exist
- `npx playwright test --list` discovers test stubs
- `grep "PANEL-0" wookiee-hub/tests/detail-panel.spec.ts` shows all 8 requirement IDs
</verification>

<success_criteria>
- Playwright config targets localhost:25000 with chromium browser
- Shared fixtures provide panel interaction helpers
- Test stubs exist for all 8 PANEL requirements, discoverable but not failing
- Subsequent plans can reference these tests in their `<verify>` blocks
</success_criteria>

<output>
After completion, create `.planning/phases/02-detail-panel/02-00-SUMMARY.md`
</output>
