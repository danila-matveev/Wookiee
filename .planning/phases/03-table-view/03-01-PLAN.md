---
phase: 03-table-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/product_matrix_api/dependencies.py
  - services/product_matrix_api/routes/models.py
  - services/product_matrix_api/routes/articles.py
  - services/product_matrix_api/routes/products.py
  - wookiee-hub/src/hooks/use-table-state.ts
  - wookiee-hub/src/lib/field-def-columns.ts
  - tests/product_matrix_api/test_routes_models.py
autonomous: true
requirements:
  - TABLE-04
  - TABLE-05

must_haves:
  truths:
    - "Backend list endpoints accept ?sort=field&order=asc|desc and return correctly sorted data"
    - "Backend list endpoints accept ?page=N&per_page=M and return paginated PaginatedResponse"
    - "useTableState hook manages page, sort, hiddenFields state and produces apiParams object"
    - "fieldDefsToColumns converts FieldDefinition[] into Column[] with correct keys, labels, and lookup resolution"
  artifacts:
    - path: "services/product_matrix_api/dependencies.py"
      provides: "CommonQueryParams with order param"
      contains: "order"
    - path: "wookiee-hub/src/hooks/use-table-state.ts"
      provides: "useTableState hook"
      exports: ["useTableState", "TableState", "TableSortState"]
    - path: "wookiee-hub/src/lib/field-def-columns.ts"
      provides: "FieldDef-to-Column converter"
      exports: ["fieldDefsToColumns"]
  key_links:
    - from: "wookiee-hub/src/hooks/use-table-state.ts"
      to: "services/product_matrix_api/dependencies.py"
      via: "apiParams maps to CommonQueryParams (page, per_page, sort, order)"
      pattern: "sort.*order"
    - from: "wookiee-hub/src/lib/field-def-columns.ts"
      to: "wookiee-hub/src/components/matrix/panel/types.ts"
      via: "LOOKUP_TABLE_MAP import for reference field resolution"
      pattern: "LOOKUP_TABLE_MAP"
---

<objective>
Backend sort/pagination wiring and frontend infrastructure hooks for Phase 3 table view.

Purpose: All subsequent table UI work (columns, pagination controls, sort indicators, create dialog) depends on the backend accepting sort/order params and on shared frontend hooks that manage table state. This plan builds those foundations.

Output: Working backend sort+pagination params on all list endpoints; `useTableState` hook; `fieldDefsToColumns` utility; backend tests for sort and pagination.
</objective>

<execution_context>
@/Users/danilamatveev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilamatveev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-table-view/03-CONTEXT.md
@.planning/phases/03-table-view/03-RESEARCH.md

@services/product_matrix_api/dependencies.py
@services/product_matrix_api/routes/models.py
@services/product_matrix_api/routes/articles.py
@services/product_matrix_api/routes/products.py
@services/product_matrix_api/services/crud.py
@wookiee-hub/src/components/matrix/data-table.tsx
@wookiee-hub/src/components/matrix/panel/types.ts
@wookiee-hub/src/lib/matrix-api.ts
@wookiee-hub/src/stores/matrix-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend sort and pagination params</name>
  <files>services/product_matrix_api/dependencies.py, services/product_matrix_api/routes/models.py, services/product_matrix_api/routes/articles.py, services/product_matrix_api/routes/products.py, tests/product_matrix_api/test_routes_models.py</files>
  <behavior>
    - test_list_models_sort_asc: GET /api/matrix/models/?sort=kod&order=asc returns items sorted by kod ascending
    - test_list_models_sort_desc: GET /api/matrix/models/?sort=kod&order=desc returns items sorted by kod descending
    - test_list_models_pagination: GET /api/matrix/models/?page=1&per_page=5 returns at most 5 items and correct total/pages in response
    - test_list_models_pagination_page2: GET /api/matrix/models/?page=2&per_page=5 returns different items than page 1
    - test_list_models_sort_invalid_order: GET /api/matrix/models/?sort=kod&order=invalid falls back to asc (no 422)
  </behavior>
  <action>
    1. In `dependencies.py`: Add `order: Optional[str] = None` field to `CommonQueryParams` dataclass. In the `common_params` function, add `order: Optional[str] = Query(None, pattern="^(asc|desc)$")` parameter. Pass it through to the dataclass constructor.

    2. In each route file (`models.py`, `articles.py`, `products.py`): Where `CrudService.get_list` is called and `params.sort` is passed, combine sort and order into the `field:dir` format that CrudService expects:
       ```python
       sort_param = f"{params.sort}:{params.order or 'asc'}" if params.sort else None
       ```
       Pass `sort_param` as the `sort` argument to `CrudService.get_list`.

    3. Write tests in `tests/product_matrix_api/test_routes_models.py` covering the behaviors above. Use the existing test client fixture pattern from the file.

    IMPORTANT: Do NOT change CrudService.get_list itself — it already handles `field:dir` format correctly. Only change the route layer that constructs the sort string.
  </action>
  <verify>
    <automated>cd /Users/danilamatveev/Desktop/Документы/Cursor/Wookiee && pytest tests/product_matrix_api/test_routes_models.py -x -q -k "sort or pagination"</automated>
  </verify>
  <done>All sort and pagination tests pass. Backend list endpoints accept ?sort=field&order=asc|desc and ?page=N&per_page=M params correctly.</done>
</task>

<task type="auto">
  <name>Task 2: useTableState hook and fieldDefsToColumns utility</name>
  <files>wookiee-hub/src/hooks/use-table-state.ts, wookiee-hub/src/lib/field-def-columns.ts</files>
  <action>
    1. Create `wookiee-hub/src/hooks/use-table-state.ts`:
       - Export `TableSortState` interface: `{ field: string | null; order: "asc" | "desc" }`
       - Export `TableState` interface with: `page`, `perPage`, `sort`, `hiddenFields: Set<string>`, `setPage`, `toggleSort`, `toggleFieldVisibility`, `apiParams`
       - Export `useTableState(entity: string, defaultPerPage = 50)` hook
       - `toggleSort(field)`: if same field, toggle asc/desc; if different field, set to asc; always reset page to 1
       - `apiParams` returns `{ page, per_page, sort?, order? }` — only include sort/order when sort.field is set
       - Use `import type` for type-only imports (verbatimModuleSyntax)

    2. Create `wookiee-hub/src/lib/field-def-columns.ts`:
       - Export `fieldDefsToColumns<T>(defs: FieldDefinition[], lookupCache, hiddenFields: Set<string>): Column<T>[]`
       - Filter by `is_visible && !hiddenFields.has(field_name)`
       - Sort by `sort_order`
       - For `_id` fields: set column key to `field_name.replace(/_id$/, "_name")` so it reads pre-joined backend data
       - For `status_id`: mark column with a `isStatus: true` flag (or use field_name detection) for badge rendering
       - Import `LOOKUP_TABLE_MAP` from `@/components/matrix/panel/types` for reference field detection
       - Import `FieldDefinition` type from `@/lib/matrix-api`
       - Carry the original `FieldDefinition` on the column object so DataTable can use it for sort logic

    Follow research Pattern 1 and Pattern 2 from 03-RESEARCH.md exactly. Use `import type` for type-only imports.
  </action>
  <verify>
    <automated>cd /Users/danilamatveev/Desktop/Документы/Cursor/Wookiee/wookiee-hub && npx tsc --noEmit src/hooks/use-table-state.ts src/lib/field-def-columns.ts 2>&1 | head -20</automated>
  </verify>
  <done>Both files compile without TypeScript errors. useTableState exports page/sort/hiddenFields management. fieldDefsToColumns converts FieldDefinition[] to Column[] with correct _name keys for reference fields.</done>
</task>

</tasks>

<verification>
- `pytest tests/product_matrix_api/test_routes_models.py -x -q -k "sort or pagination"` — all pass
- `cd wookiee-hub && npx tsc --noEmit` — no new type errors from added files
- Manual: `curl "http://localhost:8000/api/matrix/models/?sort=kod&order=desc&page=1&per_page=5"` returns sorted, paginated JSON with `total`, `page`, `pages` fields
</verification>

<success_criteria>
- Backend sort+order params work on models, articles, products endpoints
- Tests prove sort ordering and pagination correctness
- useTableState hook provides page/sort/visibility state with apiParams output
- fieldDefsToColumns maps FieldDefinitions to Column objects with human-readable keys
</success_criteria>

<output>
After completion, create `.planning/phases/03-table-view/03-01-SUMMARY.md`
</output>
