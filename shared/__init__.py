"""
Shared library — common code used by all agents and services.

Modules:
    config      — unified project configuration (.env loader)
    data_layer  — SQL queries for WB/OZON/Supabase databases
    clients/    — API clients (z.ai, WB, OZON, MoySklad, Sheets, Bitrix)
    utils/      — utilities (JSON extraction, etc.)
"""
