services:
  # ── Oleg v2: reporting orchestrator ──
  wookiee-oleg:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: wookiee_oleg
    restart: unless-stopped
    entrypoint: ["/bin/bash", "-c"]
    command:
      - |
        (echo "PATH=/usr/local/bin:/usr/bin:/bin"; echo "PYTHONPATH=/app"; echo "*/30 7-18 * * * cd /app && python scripts/run_report.py --schedule >> /proc/1/fd/1 2>&1"; echo "0 10 * * 1 cd /app && python scripts/run_search_queries_sync.py >> /proc/1/fd/1 2>&1"; echo "0 6 * * * cd /app && python scripts/sync_sheets_to_supabase.py --level all >> /proc/1/fd/1 2>&1"; echo "0 5 * * 0 cd /app && python scripts/retention_cleanup.py >> /proc/1/fd/1 2>&1") | crontab -
        echo "Wookiee cron installed: report every 30 min 07:00-18:00 MSK + search queries Mon 10:00 + sheets sync daily 06:00 + retention cleanup Sun 05:00"
        cron -f

    env_file:
      - ../.env

    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Europe/Moscow

    volumes:
      - ../agents/oleg/data:/app/agents/oleg/data
      - ../services/etl/data:/app/services/etl/data
      - ../reports:/app/reports
      - ../scripts:/app/scripts:ro

    networks:
      - n8n-docker-caddy_default

    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1536M
        reservations:
          cpus: '0.5'
          memory: 512M

  # ── Sheets Sync: ежедневная синхронизация данных в Google Sheets ──
  sheets-sync:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: wookiee_sheets_sync
    restart: unless-stopped
    command: ["python", "-m", "services.sheets_sync.control_panel"]

    env_file:
      - ../.env

    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Europe/Moscow

    volumes:
      - ../services/sheets_sync/credentials:/app/services/sheets_sync/credentials:ro

    networks:
      - n8n-docker-caddy_default

    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 128M

  # ── WB Logistics API: HTTP-эндпоинт для запуска расчёта перестановок из Google Sheets ──
  wb-logistics-api:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.wb_logistics_api
    container_name: wb-logistics-api
    restart: unless-stopped

    env_file:
      - ../.env

    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Europe/Moscow

    volumes:
      - ../services/sheets_sync/credentials:/app/services/sheets_sync/credentials:ro
      - ../services/wb_localization/data:/app/services/wb_localization/data

    networks:
      - n8n-docker-caddy_default

    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 30s

    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

  # ── WB MCP Server (ИП): Wildberries Seller API via MCP ──
  wb-mcp-ip:
    build:
      context: /opt/wildberries-mcp-server
      dockerfile: Dockerfile
    container_name: wb_mcp_ip
    restart: unless-stopped

    environment:
      - WB_API_TOKEN=${WB_API_TOKEN_IP}
      - MCP_TRANSPORT=http
      - MCP_PORT=8080

    networks:
      - n8n-docker-caddy_default

    healthcheck:
      test: ["CMD", "wget", "-qO/dev/null", "http://127.0.0.1:8080/health"]
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 10s

    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M

  # ── WB MCP Server (ООО): Wildberries Seller API via MCP ──
  wb-mcp-ooo:
    build:
      context: /opt/wildberries-mcp-server
      dockerfile: Dockerfile
    container_name: wb_mcp_ooo
    restart: unless-stopped

    environment:
      - WB_API_TOKEN=${WB_API_TOKEN_OOO}
      - MCP_TRANSPORT=http
      - MCP_PORT=8080

    networks:
      - n8n-docker-caddy_default

    healthcheck:
      test: ["CMD", "wget", "-qO/dev/null", "http://127.0.0.1:8080/health"]
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 10s

    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M

  # ── Bitrix24 MCP Server: tasks, CRM, users via MCP ──
  bitrix24-mcp:
    build:
      context: /opt/bitrix24-mcp-server
      dockerfile: Dockerfile
    container_name: bitrix24_mcp
    restart: unless-stopped

    environment:
      - BITRIX24_WEBHOOK_URL=${BITRIX24_WEBHOOK_URL}
      - MCP_TRANSPORT=http
      - MCP_PORT=8080

    networks:
      - n8n-docker-caddy_default

    healthcheck:
      test: ["CMD", "wget", "-qO/dev/null", "http://127.0.0.1:8080/health"]
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 10s

    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M

networks:
  n8n-docker-caddy_default:
    external: true
