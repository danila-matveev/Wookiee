# v3 micro-agent system dependencies
# (extends agents/oleg/requirements.txt which provides aiogram, httpx, psycopg2, etc.)

# LangGraph + LangChain (agent runner)
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0

# Scheduling (same as v2)
APScheduler==3.10.4
pytz==2024.1

# Telegram Bot (same as v2)
aiogram==3.15.0

# HTTP client (watchdog health checks)
httpx==0.27.0

# Database
psycopg2-binary==2.9.9

# Config
python-dotenv==1.0.0
