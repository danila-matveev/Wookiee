# Вопросы к разработчику БД

> **Дата:** 20 февраля 2026
> **Контекст:** По результатам исследования обеих БД (pbi_wb_wookiee, pbi_ozon_wookiee) и анализа Excel-документации метрик.

---

## 1. Семантика конкретных полей

### 1.1 abc_date (WB)

| Поле | Вопрос |
|------|--------|
| `marga` vs `marga_union` | `marga` — это полная маржа по формуле из 30+ полей? `marga_union` = `marga` / `full_counts`? Почему `marga_union - nds` даёт расхождение ~10% с PowerBI, а 11-полевая формула <1%? |
| `sale_sum` | Реализация — это `revenue - revenue_return`? Или отдельный расчёт? |
| `proverka` | "Поступления по новым статьям" — это диагностическое поле для отлова новых supplier_oper_name? |

### 1.2 abc_date (OZON)

| Поле | Вопрос |
|------|--------|
| `service_kor` | Что такое "корректировка услуг"? Входит в формулу маржи со знаком минус (уменьшает расход). Какие статьи сюда попадают? |
| `service_compens` | Компенсация услуг — от OZON продавцу? Какие статьи? |
| `transfer_delivery` | Перевод за доставку — входит в маржу со знаком плюс. Это возврат средств за логистику? |

### 1.3 Таблица sverka

| Вопрос |
|--------|
| `delta_ca` (категории) систематически = 0.18-0.19 (расхождение ~80%). Это известная проблема? Какие именно категории расходятся? |


| Формат даты: `2026-4` — это номер недели ISO? |

---

## 2. Назначение таблиц

### 2.1 kz_off (719K строк)

Таблица содержит SEO-данные по поисковым запросам (позиции, частоты, конверсии, перцентили). Вопросы:

| Вопрос |
|--------|
| Это замена пустых `wb_item_position_3` и `wb_srch_position_1`? |
| Поле `text` — это ID поискового запроса? Можно ли получить текст самого запроса? |
| Данные обновляются еженедельно? (образцы все имеют `data` = 2025-08-20) |
| `rating` = 10.0 — это шкала до 10? Или ошибка (должно быть до 5)? |




### 2.2 adv_budget_everyhour (71K строк)

| Вопрос |
|--------|
| Все 3 образца имеют cash=0, netting=0, total=0. Таблица реально заполняется? |
| Частота обновления? Для реального почасового мониторинга расхода? |

### 2.3 ms_ss_pribylnost (21K строк)

| Вопрос |
|--------|
| `sell_cost` и `return_cost` — это себестоимость из МойСклад? Как соотносится с `sebest_view`? |
| `lk` и `project` = NULL во всех образцах. Это нормально? |

### 2.4 OZON-специфичные таблицы

| Таблица | Вопрос |
|---------|--------|
| `finance_products_buyout` (228 строк) | Это отдельный учёт для финансовых продуктов OZON (Buy on credit и т.д.)? Попадают ли эти суммы в abc_date? |
| `ozon_adv_all_pvp` (142) | PVP — это product_promotion? Как соотносится с `ozon_adv` и `ozon_adv_sp`? |
| `postings_backup` (1K) | Зачем бэкап отправлений в основной БД? Это аварийная копия? |
| `ucenka_ozon` (15K) | Уценённые товары. Учитываются ли в abc_date? Как влияют на маржу? |

---

## 3. Логика заполнения (API ↔ Таблица)

### 3.1 Какие API-эндпоинты используются?

| Таблица | Предполагаемый API | Вопрос |
|---------|---------------------|--------|
| `reportdetailbyperiod` | WB API v1/supplier/reportDetailByPeriod | Подтвердите. Как часто? |
| `accrual_report_wb` | Какой эндпоинт? | Начисления формируются WB раз в неделю? |
| `content_analysis` | WB API /content/v1/analytics? | Подтвердите |
| `kz_off` | Какой API WB? | SEO-данные — Аналитика поисковых запросов? |
| `category` / `categore` (OZON) | OZON Finance API? | Операции по типам — какой эндпоинт? |
| `ozon_services` | OZON v2/posting/fbs/list или v3? | Как формируется связь posting_number → product_id → quantity? |

### 3.2 Расписание обновлений

| Вопрос |
|--------|
| По данным `manager_dashboard_app_sourceupdatemodelbd`: обновление всех таблиц WB занимает ~2 мин (11:47-11:49). Это полная перезагрузка или инкрементальная? |
| Есть ли SLA на обновление? (напр. данные за вчера доступны к 10:00) |
| `reportdetailbyperiod` (4.8M строк) — при перезагрузке может быть недоступна? |

---

## 4. Приоритетные вопросы (для улучшения работы агентов)

| # | Вопрос | Влияние на агентов |
|---|--------|--------------------|
| 1 | Формула `marga` в WB: можно ли использовать предрассчитанное `marga - nds` вместо 11-полевой формулы? Если да — экономим вычисления | Олег: упрощение SQL |
| 2 | `kz_off` — доступна ли расшифровка поискового запроса (текст, а не ID)? | Олег: SEO-аналитика по запросам |
| 3 | `detail_history_report` vs `content_analysis` — какой источник приоритетнее? | Олег: отчёты по воронке |
| 4 | ~~Планирование (`plan_article`)~~ **ОТВЕТ:** обновляется вручную через [таблицу управленческого учёта](https://docs.google.com/spreadsheets/d/1Dsz7s_mZ0wUhviGFho89lyhtjce1V0Cmv_RPL1aLxnk/edit?gid=1763584085#gid=1763584085). Планируется агент-планировщик | Олег: план-факт анализ |
| 5 | `search_stat` OZON = 0. Это known issue? Будет ли исправлено? | Олег: OZON SEO невозможен |
| 6 | ~~Доступна ли таблица Журнала операций (ЖО)?~~ **ОТВЕТ:** доступ к [Google Sheet ЖО](https://docs.google.com/spreadsheets/d/1Dsz7s_mZ0wUhviGFho89lyhtjce1V0Cmv_RPL1aLxnk/edit?gid=1763584085#gid=1763584085) выдан через серверный email | Все агенты: фулфилмент, упаковка, зеркала |
| 7 | Таблица `nalog` (ставки налога по ЛК) — существует ли отдельная таблица или hardcoded 7%? | Олег: корректный расчёт налога |

---

## 5. Известные расхождения данных

| Проблема | Детали |
|----------|--------|
| `delta_ca` в sverka = 0.18 | Систематическое расхождение категорий между БД и PowerBI (~80%). Требует расследования |
| `search_stat` OZON = 0 | Органические данные в OZON отсутствуют |
