# Data Quality Notes — Wookiee Analytics

Документ фиксирует обнаруженные проблемы качества данных, их причины и применённые исправления.
Цель — избежать повторения ошибок в будущих отчётах.

---

## 1. Регистр имён моделей (CRITICAL)

**Проблема:** В базах данных WB и OZON артикулы хранятся с разным регистром.

- **WB `abc_date.article`:** преимущественно lowercase (`wendy/S`), но встречаются Capitalized (`Wendy/S`)
- **OZON `abc_date.article`:** преимущественно Capitalized (`Wendy/S`), но встречаются lowercase

**Последствия без LOWER():**
- `SPLIT_PART(article, '/', 1)` возвращает `wendy` и `Wendy` как разные модели
- GROUP BY создаёт отдельные строки для каждого варианта
- WB ТОП-10 показывал `wendy` = 1,980,624 руб, скрывая `Wendy` = -369,760 руб (негативная маржа)
- Комбинированный рейтинг WB+OZON не объединял каналы (разные ключи)
- Итог: завышенная маржа по моделям на WB, комбинированный рейтинг = WB рейтинг

**Масштаб ошибки (январь 2026):**
| Модель | Без LOWER() | С LOWER() | Разница |
|--------|-------------|-----------|---------|
| wendy (WB) | 1,980,624 | 1,610,864 | -369,760 (-19%) |
| audrey (WB) | 755,319 | 505,712 | -249,607 (-33%) |
| ruby (WB) | 623,119 | 457,723 | -165,396 (-27%) |

**Исправление:**
- `get_wb_by_model()`: `LOWER(SPLIT_PART(article, '/', 1)) as model`
- `get_ozon_by_model()`: `LOWER(SPLIT_PART(article, '/', 1)) as model`
- Файл: `scripts/data_layer.py`

**Правило на будущее:** Все запросы с GROUP BY по имени модели ОБЯЗАНЫ использовать `LOWER()`.

---

## 2. СПП бренда (ИТОГО) = 0% (BUG)

**Проблема:** При расчёте ИТОГО (WB + OZON) поле `spp_pct` было захардкожено в 0:
```python
t['spp_pct'] = 0  # БЫЛ БАГ
```

**Причина:** При суммировании каналов нельзя просто сложить проценты СПП — нужен средневзвешенный.

**Исправление:**
```python
total_spp_amount = wb_spp_amount + ozon_spp_amount
t['spp_pct'] = (total_spp_amount / total_revenue_before_spp * 100) if total_revenue > 0 else 0
```

**Правило на будущее:** Процентные метрики (СПП%, маржинальность, ДРР) нельзя суммировать напрямую при объединении каналов — только через средневзвешенное от абсолютных значений.

---

## 3. Себестоимость/ед за сравниваемый период = "—" (BUG)

**Проблема:** В декомпозиции по 5 рычагам себестоимость за предыдущий период отображалась как "—" вместо числа.

**Причина:** `sebes_prev` рассчитывался, но не сохранялся в словаре гипотезы H1. Рендер использовал захардкоженное "—".

**Исправление:** Добавлен `sebes_prev` в гипотезу, рендер показывает `format_num(h1['sebes_prev'])`.

---

## 4. Трафик (просмотры карточек) завышен vs PowerBI

**Проблема:**
- Отчёт: 1,813,155 просмотров (январь 2026)
- PowerBI (ООО+ИП): 1,473,410 просмотров
- Расхождение: +23%

**Частичное исправление:**
- Добавлен фильтр `content_analysis.vendorcode IN (SELECT DISTINCT article FROM abc_date ...)` — отсекает артикулы без финансовых данных
- Эффект: снизило с 1,813,862 до 1,813,155 (минимальный эффект)

**Остаточное расхождение (23%):**
Таблица `content_analysis` имеет поле `lk` (юрлицо) со значениями:
- `WB ИП Медведева П.В.`
- `WB ООО ВУКИ`

Возможные причины оставшегося расхождения:
1. PowerBI может применять дополнительные фильтры по `lk` или по списку артикулов
2. `content_analysis` может содержать данные по артикулам, которые есть в `abc_date`, но исключены из воронки PowerBI
3. Разный способ подсчёта `opencardcount` (кард-опены vs просмотры)

**Действие:** Требуется уточнение у разработчика БД / аналитика PowerBI:
- Какой фильтр по `lk` используется в PowerBI?
- Есть ли дополнительный фильтр по списку артикулов?
- Совпадает ли определение "просмотров" в PowerBI и `opencardcount`?

---

## 5. Корзины и заказы в воронке — аналогично п.4

Поля `addtocartcount`, `orderscount`, `buyoutscount` из `content_analysis` подвержены тем же расхождениям, что и `opencardcount`. Причина та же — отсутствие фильтра по юрлицу.

---

## 6. Известные особенности данных

### WB
- `revenue_spp` = выручка ДО скидки площадки (не интуитивно, суффикс `_spp` = "before SPP")
- `revenue` = выручка ПОСЛЕ скидки площадки (для клиента)
- `spp` = сумма скидки в рублях
- `full_counts` = продажи (факт выкупа), `count_orders` = заказы

### OZON
- `price_end` = выручка ДО скидки площадки (аналог WB `revenue_spp`)
- `price_end_spp` = выручка ПОСЛЕ скидки
- `marga - nds` = маржа (верифицировано точно)
- Органический трафик (`search_stat`) = 0 (API не настроен)
- Рекламные просмотры (`adv_stats_daily.views`) — только на уровне кампании, не SKU

### Supabase (товарная матрица)
- `artikuly.artikul` может не совпадать с `abc_date.article` по формату
- Привязка статусов через `modeli_osnova.kod` — приблизительная (по первой части артикула)

---

## 7. WB retention == deduction (дубликация пайплайна) — CRITICAL

**Обнаружено:** 2026-02-10, при сверке отчёта за 09.02.2026 с Power BI

**Проблема:** На дату 09.02.2026 поля `retention` и `deduction` в `abc_date` (WB) содержат **идентичные значения** в каждой из 1668 строк:
- `SUM(retention)` = 21,619.3 руб
- `SUM(deduction)` = 21,619.3 руб
- Совпадение: 1668/1668 строк (100%)

На всех остальных днях февраля 2026 эти поля различаются:
| Дата | retention | deduction | Одинаковые? |
|------|-----------|-----------|-------------|
| 01.02 | 3,955 | 3,411 | нет |
| 02.02 | 11,748 | 5,197 | нет |
| 08.02 | 12,162 | 4,392 | нет |
| **09.02** | **21,619** | **21,619** | **ДА** |

**Последствия:**
- Формула маржи WB вычитает оба поля: маржа занижена на SUM(deduction) = 21,619 руб
- Отчёт показывал маржу бренда 182,799 руб, Power BI показывает ~200,000 руб
- Расхождение: -8.6% (вместо штатных <1%)

**Исправление (автоматическое):**
- Функция `validate_wb_data_quality()` в `data_layer.py` детектирует дубликацию
- `daily_analytics.py` при обнаружении корректирует маржу: +SUM(deduction)
- В отчёт добавляется предупреждение о качестве данных

**Корневая причина:** Баг пайплайна загрузки данных в PostgreSQL. Требуется исправление на стороне ETL.

**Правило:** Всегда проверять `retention != deduction` при сверке с Power BI. При совпадении — один из столбцов содержит дубль.

---

## Чек-лист для новых отчётов

- [ ] Все GROUP BY по модели используют `LOWER(SPLIT_PART(...))`
- [ ] Процентные метрики при объединении каналов — только средневзвешенные
- [ ] Все значения для сравниваемого периода реально отображаются (не "—")
- [ ] Комбинированный рейтинг ≠ WB рейтинг (проверить что OZON вошёл)
- [ ] Сумма моделей в комбинированном рейтинге ≈ сумма в Уровне 1
- [ ] Трафик: помнить о расхождении ~20% с PowerBI до выяснения фильтров
- [ ] WB retention и deduction: проверить что они не дублируются (validate_wb_data_quality)
- [ ] Выкуп % показан как информационный (лаг 3-21 дн.), НЕ как причина изменения маржи
- [ ] ДРР имеет разбивку: внутренняя vs внешняя реклама
- [ ] Анализ рекламы включает корреляцию с заказами (эффективность)
- [ ] СПП % бренда (ИТОГО) = средневзвешенный, не 0
- [ ] OZON выручка/продажи: сверять с дашбордом (известное расхождение ~7%)

---

---

## 8. Supabase Security — RLS не был включён (CRITICAL)

**Обнаружено:** 2026-02-11, письмо Supabase Security Advisor (25 errors)

**Проблема:** При развертывании схемы `wookiee_sku_database` в Supabase **не был включён RLS** (Row Level Security) ни на одной из 16 таблиц. Роли `anon` и `authenticated` имели полный доступ (SELECT, INSERT, UPDATE, DELETE, TRUNCATE) ко всем таблицам и views через публичный REST API Supabase.

**Масштаб:** Кто угодно с `anon key` проекта мог через REST API:
- Читать все данные товарной матрицы (артикулы, баркоды, цены, поставщики)
- Модифицировать или удалять любые записи
- Вызывать публичные функции

**Исправление (миграция 005):**
1. RLS включён на всех 16 таблицах
2. Роль `anon` — все права отозваны
3. Роль `authenticated` — ограничена до SELECT only
4. Созданы 32 RLS-политики
5. Default privileges настроены на запрет для `anon`

**Файл:** `sku_database/scripts/migrations/005_fix_supabase_security.py`

**Правило:** При создании новой таблицы в Supabase — обязательно RLS + политики (см. `sku_database/README.md`).

---

## 9. OZON расхождение выручки/продаж с дашбордом

**Обнаружено:** 2026-02-11, при проверке отчёта за 09.02.2026

**Проблема:** Отчёт показывает OZON за 09.02: выручка до СПП 156 837 руб / 74 шт продаж. Дашборд PowerBI: 147 134 руб / 69 шт.
Расхождение: +6.6% по выручке, +7.2% по штукам.

**Возможные причины:**
1. OZON `abc_date` может содержать данные по артикулам, исключённым из дашборда PowerBI
2. Разные фильтры по юрлицу (`lk`) в PowerBI vs наш запрос (нет фильтра по `lk`)
3. Разное определение "продаж" (`count_end` vs другой способ подсчёта в PowerBI)

**Действие:** Уточнить у разработчика БД / аналитика PowerBI:
- Какой фильтр по `lk` применяется в PowerBI для OZON?
- Совпадает ли выборка артикулов?
- Совпадает ли определение "продаж"?

**Правило:** Контролировать расхождение OZON при сверке отчётов. На малой базе OZON (~15% выручки) даже небольшое расхождение заметно.

---

## 10. WB: 11-полевая формула маржи не учитывала возвраты (CRITICAL)

**Обнаружено:** 2026-02-11, при сверке отчёта за 10.02.2026 с OneScreen

**Проблема:** Формула маржи WB из 11 полей (`revenue_spp - comis_spp - logist - sebes - reclama - reclama_vn - storage - nds - penalty - retention - deduction`) давала 245 207 руб, а OneScreen — 239 199 руб. Расхождение: +2.51%.

**Корневая причина:** 11-полевая формула НЕ учитывает возвраты. Поле `revenue_spp` включает выручку по возвращённым товарам, но `revenue_return_spp` (11 304 руб) не вычитается. Аналогично `sebes` включает себестоимость возвратов (`sebes_return` = 2 074 руб), а `logist` — логистику возвратов (`logis_return_rub` = 250 руб).

**Правильная формула WB:** `SUM(marga) - SUM(nds) - SUM(reclama_vn)`
- Поле `marga` рассчитывается на стороне WB и уже учитывает все возвраты
- Аналогично OZON: `SUM(marga) - SUM(nds)` — но WB дополнительно вычитает `reclama_vn` (внешняя реклама)
- Верификация: 239 199.23 руб vs OneScreen 239 199 руб (расхождение 0.23 руб, < 0.001%)

**Также исправлено:**
- `SUM(revenue_spp)` → `SUM(revenue_spp) - COALESCE(SUM(revenue_return_spp), 0)` для чистой выручки
- Верификация: 1 120 531.3 руб vs OneScreen 1 120 531 руб (расхождение 0.3 руб)

**Не исправлено (известное ограничение):**
- WB `full_counts` — gross продажи (включая возвраты). Поле `count_return` НЕ СУЩЕСТВУЕТ в таблице `abc_date` (несмотря на документацию в DATABASE_REFERENCE.md). Чистые продажи WB отличаются от OneScreen на ~5 шт (< 1%).

**Файл:** `scripts/data_layer.py` — `WB_MARGIN_SQL`, все функции `get_wb_*`

**Правило:** WB маржа = `marga - nds - reclama_vn`. НЕ использовать 11-полевую формулу. WB выручка до СПП = `revenue_spp - revenue_return_spp`.

---

## 11. WB: маппинг внутренняя/внешняя реклама (reclama / reclama_vn)

**Обнаружено:** 2026-02-23, таблица «1.1 Ключевые метрики» (Notion): «Реклама внутренняя» и «Реклама внешняя» оказались перепутаны; внутренняя реклама показывала только WB, внешняя не сходилась с PowerBI.

**Причина:** После обновления ETL WB поля снова соответствуют схеме (`reclama` = внутренняя реклама МП, `reclama_vn` = внешняя реклама/блогеры), но в `shared/data_layer.py` оставался прошлый workaround (внутренняя = `reclama_vn`, внешняя = `reclama`), из-за чего таблица 1.1 собиралась с инверсией и без OZON внутренней рекламы.

**Исправление:** Вернули штатный маппинг: `adv_internal = SUM(reclama)`, `adv_external = SUM(reclama_vn)` во всех WB-запросах `shared/data_layer.py` (finance/by_model/by_article/daily_series/price_margin/fin_data_by_barcode).

**Проверка:** Сверить «Реклама внутренняя/внешняя» в PowerBI/OneScreen и в таблицах 1.1, 5.1, 5.2 на следующих данных (после 23.02.2026). При повторном расхождении — проверить ETL заполнение `reclama`/`reclama_vn` и откатить.

---

## 12. DATABASE_REFERENCE.md: поле count_return не существует

**Обнаружено:** 2026-02-11

**Проблема:** `DATABASE_REFERENCE.md` документирует поле 19 как `count_return` (количество возвратов) в WB `abc_date`. Однако при запросе `information_schema.columns` это поле **не найдено** в таблице. Поля, связанные с возвратами, которые реально существуют: `revenue_return_spp`, `revenue_return`, `sebes_return`, `logis_return_rub`, `returns` (руб).

**Последствие:** Невозможно вычислить чистые продажи WB в штуках (`full_counts - count_return`). Расхождение с OneScreen ~5 шт (~0.7%).

**Действие:** Обновить `DATABASE_REFERENCE.md` — пометить `count_return` как несуществующее. Уточнить у разработчика БД.

---

## 13. Ложноположительное уведомление "Данные готовы" (BUG)

**Обнаружено:** 2026-02-18

**Проблема:** Агент Олег отправил уведомление "Данные за 17.02.2026 готовы", но по Power BI данные WB по марже доступны только до 16.02. Уведомление было преждевременным — маржинальные данные за 17.02 загружены не полностью.

**Корневая причина:** Слишком мягкие пороги в `DataFreshnessService._check_mp()`:
- Порог выручки: 50% от предыдущего дня (недостаточно строго)
- Порог строк с маржой: 50% от предыдущего дня (недостаточно строго)
- Отсутствовала проверка абсолютного заполнения маржи (доля от общего числа строк)
- Отсутствовала санитарная проверка соотношения маржа/выручка

ETL-пайплайн загрузил строки с revenue из API маркетплейса, но marga ещё не была полностью рассчитана. Проверка с порогом 50% пропустила частично загруженные данные.

**Исправление:**

| Проверка | Было | Стало |
|----------|------|-------|
| Выручка vs пред. день | 50% | 70% |
| Строки с маржой vs пред. день | 50% | 90% |
| Строки с маржой vs всего строк | — | 90% (новое) |
| Маржа/выручка ratio | — | 5% (новое) |
| MAX(date) = вчера | — | exact match (новое) |

**Файлы:** `agents/oleg/services/data_freshness_service.py`, `scripts/check_data_ready.py`

**Правило:** При проверке готовности данных пороги заполнения маржи должны быть >= 90%. Частичная загрузка финансовых данных не должна считаться "готовой".

---

*Создано: 2026-02-09*
*Обновлено: 2026-02-10 — добавлены п.7, аналитические правила (выкуп-лаг, ДРР-разбивка, реклама-заказы, СПП)*
*Обновлено: 2026-02-11 — добавлен п.8, критическая уязвимость безопасности Supabase (RLS)*
*Обновлено: 2026-02-11 — добавлен п.9, расхождение OZON с дашбордом*
*Обновлено: 2026-02-11 — добавлены п.10-11, WB формула маржи (возвраты), count_return не существует*
*Обновлять при обнаружении новых проблем качества данных.*
