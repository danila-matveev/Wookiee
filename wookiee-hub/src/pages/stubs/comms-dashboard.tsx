import { LayoutDashboard } from "lucide-react"
import { ModuleStub } from "@/components/shared/module-stub"

export function CommsDashboardPage() {
  return <ModuleStub icon={LayoutDashboard} title="Дашборд коммуникаций" description="Агрегированная картина по отзывам — метрики, тренды и разбивка по магазинам." />
}
