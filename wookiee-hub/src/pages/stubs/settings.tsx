import { Settings } from "lucide-react"
import { ModuleStub } from "@/components/shared/module-stub"

export function SettingsPage() {
  return <ModuleStub icon={Settings} title="Настройки" description="Доступы, роли, модули." />
}
