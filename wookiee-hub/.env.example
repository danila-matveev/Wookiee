# Supabase (Phase 1.5: real data fetching)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Marketing module rollout (Phase 5 prod flip — keep false on main until QA pass)
VITE_FEATURE_MARKETING=false

# analytics_api: marketing sync trigger + status (Task B.2.2 UpdateBar refresh).
# Leave blank to use same-origin requests; set to http://localhost:8001 for local dev.
VITE_ANALYTICS_API_URL=
VITE_ANALYTICS_API_KEY=
